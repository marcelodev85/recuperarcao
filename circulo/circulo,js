function calculaAreaCircunferencia(raio) {
  const area = 2 * Math.PI * raio;
  return area;
}

let circunferencia = calculaAreaCircunferencia(10);

console.log(`A area da circunferencia é ${circunferencia} `);
